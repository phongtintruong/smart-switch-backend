export enum JOB_TYPE {
  NOW = 'NOW',
  ONE_TIME = 'ONE_TIME',
  PERIOD = 'PERIOD',
}

export enum JOB_PERIOD_TYPE {
  SECOND = 'SECOND',
  MINUTE = 'MINUTE',
  DAY = 'DAY',
  WEEK = 'WEEK',
  MONTH = 'MONTH',
}

export enum JOB_ACTION {
  ON = 'ON',
  OFF = 'OFF',
  FLIP = 'FLIP',
}

export enum SWITCH_STATUS {
  ON = 'ON',
  OFF = 'OFF',
}
